import Foundation

/// CloudKit container configuration constants.
public enum CloudKitConfig {
    /// The shared CloudKit container identifier.
    /// All companion apps MUST use this same identifier in their entitlements.
    public static let containerIdentifier = "iCloud.com.sebastianmurphy.PARAFoundation"
}
